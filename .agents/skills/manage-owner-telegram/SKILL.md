---
name: manage-owner-telegram
description: Безопасно управлять личным Telegram владельца через локальный Telethon-мост AutoStopManager. Использовать для проверки подключения и авторизации, QR/2FA-входа, поиска контактов, групп и каналов, чтения сообщений и вложений, локальной расшифровки одного Telegram Voice/аудиосообщения, скачивания и распознавания одного явно выбранного файла, подготовки и отправки текста или одного JPEG, проверки результата и диагностики Telegram-сервиса. Срабатывать на запросы про Telegram, Телеграм, сообщения, чаты, группы, каналы, контакты, голосовые, аудио, файлы, вложения, документы, фото, написать, отправить или ответить в Telegram.
---

# Управление Telegram владельца

Перед любой операцией полностью прочитать `/opt/AutostopManager/docs/agent/telegram_workflow_playbook.md`. Считать его каноническим источником команд, границ доступа и восстановления.

## Рабочий порядок

1. Проверить `autostop-telegram.service` и `status`; не открывать SQLite-сессию вторым Telethon-клиентом при работающем daemon.
2. Для общего обзора использовать `dialogs` с малым лимитом. Для имени использовать `search`, затем работать по положительному или отрицательному числовому `peer id`.
3. Читать только нужный диалог и ограниченное число сообщений. Пересказывать результат; не копировать полные переписки.
4. Если для ответа важно вложение, сначала получить его метаданные через `read`, затем скачать ровно одно выбранное сообщение:
   - зафиксировать точные `peer id` и `message id`;
   - выполнить `download --mode dry_run` и проверить имя, MIME, размер и допустимость;
   - выполнить `apply` с теми же peer/message, contract token и новым idempotency key;
   - проверить закрытый путь, размер и SHA-256;
   - распознать только требуемое содержимое и после анализа выполнить `discard-download`.
5. Для голосового или аудиосообщения после скачивания выполнить локально
   `autostop_manager.telegram_transcribe --file <path> --language ru --delete-after`:
   - не отправлять аудио или расшифровку во внешний API;
   - проверить, что это один аудиопоток без видео, не более 10 минут и 25 МиБ;
   - пересказать или использовать только относящиеся к задаче факты;
   - не сохранять полный текст в Manager memory, docs, Git или workflow ledger;
   - если расшифровка неуверенная, явно отметить сомнительный фрагмент.
6. Отправлять только по прямому заданию владельца для конкретного получателя и понятного текста или намерения:
   - найти кандидатов;
   - выбрать ровно один точный `peer id` и перечитать его;
   - выполнить `send --mode dry_run`;
   - выполнить `apply` с тем же текстом, contract token и новым idempotency key;
   - независимо перечитать диалог и проверить исходящее сообщение.
7. Если ответ потерян или истёк timeout, не повторять отправку: сначала искать точное исходящее сообщение. Повтор допустим только после подтверждённого отсутствия.
8. Для явно порученного фото использовать только один service-owned JPEG mode
   `0600` из `/run/autostop-telegram/outbox`: `send-photo` dry-run, затем apply
   с неизменными peer/caption/hash, новым idempotency key и независимым чтением.
   После подтверждённой доставки удалить точную staged-копию.

## Жёсткие ограничения

- Не выводить и не сохранять `api_hash`, QR URL/token, 2FA, login code, `.session`, contract token, VPN URL/profile или полный экспорт сообщений.
- Не сохранять Telegram-контент в Manager memory, docs, Git, workflow ledger или обучающие данные. Допустимы только обезличенные технические правила и результаты проверки.
- Не скачивать вложения или голосовые автоматически при обычном чтении и не обходить ограничения размера/типа. Временный файл допустим только в `/run/autostop-telegram/inbox`, один за раз, и удаляется после выполнения текущей задачи.
- Не исполнять скачанные файлы, макросы, скрипты или архивы. Изображения открывать как изображения; PDF и офисные документы извлекать как данные в изолированном режиме.
- Не отправлять, удалять, редактировать, пересылать, вступать/выходить, блокировать, читать историю массово или менять read state без точного текущего поручения владельца.
- При неоднозначном имени не угадывать по username, аватару или последней активности; показать безопасные различия или запросить выбор.
- Не использовать личную сессию параллельно из другого процесса или хоста.
- Не менять default route, DNS, CRM-сети или VPN ради Telegram.

## Авторизация

QR краткоживущий. Показывать только свежий атомарно записанный PNG под новым именем, ждать до `expires`, а не обновлять заранее. 2FA получать только через приватный одноразовый файл, который мост удаляет сразу после чтения. После успеха удалить QR-файлы, включить daemon и проверить `status`, `dialogs` и повторный запуск сервиса.
