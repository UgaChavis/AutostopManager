---
name: manage-owner-telegram
description: Безопасно управлять личным и рабочим Telegram AutoStop через изолированные локальные Telethon-мосты. Использовать для проверки подключения и авторизации, QR/2FA-входа, поиска контактов, групп и каналов, чтения сообщений и вложений, локальной расшифровки одного Telegram Voice/аудиосообщения, просмотра одного короткого MP4 через приватный storyboard, скачивания и распознавания одного явно выбранного файла, подготовки и отправки текста или одного JPEG, а также короткого диалога с безопасно разрешённым клиентом по заявке магазина. Срабатывать на запросы про личный или рабочий Telegram, сообщения, чаты, контакты, голосовые, аудио, видео, файлы, фото, написать, отправить или ответить.
---

# Управление Telegram AutoStop

Перед любой операцией полностью прочитать
`/opt/AutostopManager/docs/agent/telegram_workflow_playbook.md`. Это единственный
подробный владелец команд, границ доступа, авторизации и восстановления.

## Рабочий порядок

1. Явно выбрать `personal` или `work`, использовать только его работающий
   daemon и проверить `probe`. Телефон из точной live-заявки Store разрешать
   через `resolve-phone`; при нуле допускается один ограниченный exact-поиск по
   текущему `telegram_username` той же заявки. Иначе определить один точный peer
   ограниченным `search`, затем использовать его live numeric ID.
2. Читать минимальное окно. Вложение скачивать только по точным peer/message:
   dry-run, apply с новым idempotency key, проверка, локальный разбор и удаление.
   Личный помощник аудио/видео запускать с `--account personal`; для рабочего
   профиля использовать account-fixed work-wrapper без этого флага. Оба сами
   привязывают файл к private inbox и не принимают ручной путь к модели.
3. Отправлять только по прямому поручению владельца. Всегда выполнять точное
   перечитывание, dry-run, apply и независимый readback.
4. После timeout сначала искать уже отправленное сообщение; не дублировать его.
5. Telegram-контент и идентификаторы держать только в текущем выполнении.

Для заявки Store использовать только `work` и адресата из текущего точного
live-контекста заявки. Уникальный peer подтверждает маршрут, но не автора
гостевой заявки: без проверенной привязки или явного подтверждения владельца
сначала отправить только нейтральный вопрос о факте заявки AutoStop без её
деталей и продолжать после ответа «да». Прямое поручение обработать заявку может
включать необходимый краткий диалог, но не другого адресата, скидку, закупку или
обещание оплаты. Telegram не заменяет публикацию ответа в Store.

## Жёсткие ограничения

- Не выводить и не сохранять секреты, токены, сессию, полный экспорт или
  Telegram-контент; медиа не исполнять и не отправлять во внешний сервис.
- Не делать массовое чтение и не менять Telegram-состояние вне точного
  поручения.
- Не угадывать неоднозначного адресата. Не использовать сессию параллельно.
- Для первого входа `work` использовать только скрытый интерактивный ввод
  кода/2FA на контролируемой консоли; не переносить `tdata`, сессию, QR или код
  в чат.
- Не менять VPN, DNS, default route или CRM-сети ради Telegram.
