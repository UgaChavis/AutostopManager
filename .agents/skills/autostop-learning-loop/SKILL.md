---
name: autostop-learning-loop
description: "Проверять результат задачи AutoStop, сохранять обезличенный опыт и исправлять воспроизводимые локальные дефекты. Использовать только при effective mode=learning или прямой просьбе владельца об обучении."
---

# AutoStop Learning Loop

Используй цикл только в режиме `learning`. После обычного результата вызови
`post_run_review`, затем закрой candidate через `agent_learning_workflow` как
`no_change`, короткий обезличенный `lesson`, воспроизведённый и проверенный
`repair` либо `defer` с рабочим обходом.

Сохраняй только технические refs и переносимый обезличенный вывод. Не сохраняй
prompt, live payload, переписку, идентификаторы, суммы или секреты. Внешний
timeout или единичная самооценка сами по себе не доказывают дефект. Финансы и
release требуют отдельного поручения.
